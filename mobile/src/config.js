// Production API - HTTPS is live on ubrano.com.hr.
export const API = 'https://ubrano.com.hr/api';

// Phase 2 "Notify Nearby Buyers" - built, intentionally OFF until paid boosts
// are ready (Phase 4). Keep in sync with backend NOTIFICATIONS_ENABLED and
// the web frontend's config.js.
export const FEATURES = {
  notifications: false,
};
